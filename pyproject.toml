[project]
name = "com-kryon-weather"
version = "1.0.0"
description = "A weather widget for Class Widgets 2 with real-time conditions and CMA weather alerts."
authors = [
    {name = "松峰"},
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["class-widgets-sdk>=0.4.2"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
